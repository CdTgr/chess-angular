import {
  AlphabetType,
  ChessLayout,
  NumberType,
  TeamType,
} from '../../services/types';

export const getHighlightForKnight = (
  data: ChessLayout,
  team: TeamType,
  alpha: AlphabetType,
  num: NumberType
) => {
  const targets: [AlphabetType, NumberType][] = [];

  return targets;
};
